let currentPokemonId = null;

document.addEventListener("DOMContentLoaded", () => {
    const MAX_POKEMON = 1304;
    const pokemonID = new URL (window.location.search).get("id");
    const id = parseInt(pokemonID, 10); //turns it into a number 

});